//In javascript the current time and date is set is set using the date object
const now = new Date()
console.log(now)
//the object we create using date method can be combined we sub methods
console.log(now.getDay())
console.log(now.getDate())
console.log(now.getFullYear())
console.log(now.getMonth())
console.log(now.getHours())
  //there are many more methods they can associate with objects
  console.log()